# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Todo {
  id: ID!
  text: String!
  done: Boolean!
  user: String!
}

type User {
  id: ID!
  name: String!
  age: Int!
}

type Query {
  todos: [Todo!]!
  users: [User!]!
}